<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link rel="stylesheet" href="/css/client-style.css">
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>

    <header class="client-header">
        <nav class="container">
            <a href="/" class="logo-link">HP TECHNOLOGY</a>
            <ul class="nav-links">
     
                <li><a href="/">Home</a></li>
                <li><a href="/services">Services</a></li>
                <li><a href="/contact">Contact Support</a></li>
                
                <% if (typeof user !== 'undefined' && user) { %>
                    <li><span class="welcome-msg">Welcome, <%= user.name %></span></li>
                    
                    <li><a href="/auth/logout" class="nav-button logout-button">Log Out</a></li>
                
                <% } else { %>
                    <li><a href="/auth/login" class="nav-button">Login</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    
    <main class="container">